import { startFromHomePage } from "../../support/startUtils";

describe("Login Scenario", () => {
  beforeEach(() => {
    startFromHomePage();
  });

  it("Should be able to see something", () => {});
});
